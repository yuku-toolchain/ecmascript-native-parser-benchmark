{
	"name": "ecmascript-parser-benchmark-native",
	"type": "module",
	"private": true,
	"scripts": {
		"bench": "bun scripts/load-files.ts && bun run build && bun scripts/run-bench.ts && bun readme",
		"build": "bun build:zig && bun build:rust",
		"build:zig": "cd zig && zig build --release=fast && mkdir -p ../bin && cp ./zig-out/bin/zig ../bin/zig",
		"build:rust": "cd rust && cargo build --release && mkdir -p ../bin && cp ./target/release/rust_parsers ../bin/rust",
		"readme": "bun scripts/generate-readme.ts",
		"update-parsers": "cd rust && cargo update && cd .. && cd zig && zig fetch --save git+https://github.com/yuku-toolchain/yuku/#HEAD"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"@types/node": "^25.0.10"
	},
	"dependencies": {
		"chart.js": "^4.5.1",
		"chartjs-node-canvas": "^5.0.0"
	}
}
